[defaults]
inventory            = inventory.ini
roles_path           = roles
host_key_checking    = True
# `yaml` lives in community.general; `default` ships with ansible-core.
stdout_callback      = default
display_skipped_hosts = False
interpreter_python   = auto_silent

[ssh_connection]
pipelining = True
